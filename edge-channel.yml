server:
  port: 8003

spring:
    jpa:
      show-sql: true
      hibernate:
        ddl-auto: create-drop
        dialect: org.hibernate.dialect.MySQL5Dialect
    datasource:
      url: jdbc:mysql://localhost:3306/smart_school?useSSL=true
      username: root
      password: welcome
      testWhileIdle: true
      validationQuery: SELECT 1
      initialization-mode: always
